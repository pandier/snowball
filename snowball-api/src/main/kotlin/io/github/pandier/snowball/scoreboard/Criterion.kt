package io.github.pandier.snowball.scoreboard

/**
 * An [Objective] criterion that determines scores inside the objective and tracks statistical game elements.
 */
public interface Criterion {
    public val defaultRenderType: Objective.RenderType
}
